# Open work log (one line per pass)

- 2026-09-24 Workspace loss: the cloud workspace (git repo /home/claude/axbx) was wiped; restored the NBME-build page (201 briefs, 1,942 items) as the baseline commit and rebuilt the toolchain (pagelib, gate + allowlist, render.js on jsdom 24, edit contract with apply/verify, idgen, nbme_match, build.py, ship.sh, worker packet rules + coverage map + make_packet, session_usage); history before today, SPEC files, RUBRIC/runs and NBME coverage rows were not recoverable.
